import React from "react";

const Map = (props)=> {

    return (
    <div className="map-container">
       <h1>This is a map page</h1>
       <p> Placehodler for map</p>
    </div>);
}

export default Map;
